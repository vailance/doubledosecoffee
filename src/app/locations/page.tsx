import LocationsPageContent from '@/components/locations/LocationsPageContent';

export const metadata = {
  title: 'Locations | Double Dose Coffee',
  description:
    'Visit Double Dose Coffee in Melaka with quick access to directions, opening hours, contact details, and an embedded OpenStreetMap view.',
};

export default function LocationsPage() {
  return <LocationsPageContent />;
}
